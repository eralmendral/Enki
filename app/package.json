{
  "name": "app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "check": "biome check --write",
    "dev": "rsbuild dev --open",
    "format": "biome format --write",
    "preview": "rsbuild preview",
    "test": "vitest --run",
    "pre-commit": "biome check --write --no-errors-on-unmatched",
    "prepare": "husky",
    "lint": "biome lint",
    "lint:fix": "biome lint --write",
    "format:check": "biome format --check"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.3",
    "@rsbuild/core": "^1.5.2",
    "@rsbuild/plugin-react": "^1.4.0",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "@vitest/coverage-v8": "^3.2.4",
    "husky": "^9.1.7",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
